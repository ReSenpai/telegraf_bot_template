/**
 * If you put your token, you will need to change the ID of all images and animations
 *  in the DATA folder to your own
 */
const TOKEN = 'YOU_TOKEN';
const logLvl = 3;
const type = 'color' // log type
const DB_URL = 'you DB url adress';

module.exports = { TOKEN, TEST_TOKEN, logLvl, DB_URL, type }